{
  "name": "product-survey",
  "version": "1.0.0",
  "description": "In-app feedback.",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html",
    "build": "parcel build index.html",

    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url https://github.com/e-limu-Games/product-survey/",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "Femi Ayodeji <femi@e-limu.org>",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^1.12.3"
  },
  "dependencies": {
    "firebase": "^9.4.1",
    "gh-pages": "^4.0.0"
  }
}
